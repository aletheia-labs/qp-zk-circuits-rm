[package]
name = "wormhole-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = { workspace = true, features = ["std"] }
wormhole-circuit = { path = "../circuit", default-features = false, features = [
    "std",
] }
wormhole-prover = { path = "../prover", default-features = false }
plonky2 = { workspace = true, features = ["default"] }
zk-circuits-common = { path = "../../common" }
hex = { workspace = true, features = ["alloc"] }
